[project]
name = "alano-cut"
version = "0.4.0"
description = "Capability-routed agent harness for transcript-driven rough cuts"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = [
    "requests",
    "pillow",
    "numpy",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
